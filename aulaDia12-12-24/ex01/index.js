const greet = () => {
    alert('Hello World');
};

// console.log(greet()); Chamada Imediata
// setTimeout(greet, 2000); Chamada após 2 segundos unica vez
// setInterval(greet, 2000); Chamada a cada 2 segundos repetidamente