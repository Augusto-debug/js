let numeroPositivo = prompt("Por favor digite um numero maior do que 0");
let nomeCompleto = prompt("Por favor seu nome completo");

for(let i = 0; i < numeroPositivo; i++) {
    document.write(`<p>${nomeCompleto}</p>`)
}